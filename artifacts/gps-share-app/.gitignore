.dart_tool/
.pub-cache/
.pub/
build/
.flutter-plugins
.flutter-plugins-dependencies
.packages
pubspec.lock

# Local environment
.metadata
local.properties

# IDE
.idea/
*.iml
.vscode/

# Secrets — never commit
android/app/google-services.json
ios/Runner/GoogleService-Info.plist
# NOTE: lib/firebase_options.dart IS checked in as a placeholder stub.
# `flutterfire configure` will overwrite it locally with your real values;
# do NOT commit the regenerated version.
