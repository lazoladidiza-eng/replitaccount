kind = "web"
previewPath = "/"
title = "ClipCleaner"
version = "1.0.0"
id = "artifacts/clipcleaner"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25938

[services.development]
run = "pnpm --filter @workspace/clipcleaner run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/clipcleaner", "run", "build" ]
publicDir = "artifacts/clipcleaner/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25938"
BASE_PATH = "/"
